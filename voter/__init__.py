import pygame
from pygame import *

screen_size = (700, 500)
screen = pygame.display.set_mode(screen_size)
pygame.display.set_caption("Voter App")

# clock = pygame.Clock()


while True:
    pygame.display.update()